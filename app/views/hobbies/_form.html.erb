<%= form_with(model: hobby, local: true) do |form| %>
  <% if hobby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hobby.errors.count, "error") %> prohibited this hobby from being saved:</h2>

      <ul>
      <% hobby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="actions">
  <div id="hobbyform">
    <%= f.label :reading, "Whats your favorite book?" %> <br>
    <%= f.text_field :reading, autofocus: true %>

  </div>
</div>
  <% end %>


  t.bigint "user_id"
  t.string "reading"
  t.string "gym"
  t.string "movie"
  t.string "dancing"
  t.string "outside"
  t.string "food"
  t.string "drinks"
  t.string "museums"
